project Hamming is
   for Source_Dirs use (".");
   for Object_Dir use "obj";
   for Exec_Dir use "bin";
   for Main use ("main.adb", "tests.adb");

   package Compiler is
      for Default_Switches ("Ada") use ("-gnatwa", "-gnat12", "-O2");
   end Compiler;
end Hamming;
